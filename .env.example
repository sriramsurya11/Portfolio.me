# Optional: MongoDB connection URI for persistent cloud database storage
# If left empty, the portfolio uses a built-in memory/local storage fallback with sample doodles
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/portfolio?retryWrites=true&w=majority
PORT=5000
